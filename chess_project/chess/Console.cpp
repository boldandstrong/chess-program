#include "Board.h" 
#include "Player.h"

int main()
{
	string userInput = "";
	Board board();

	Player white(board, "white", "e1");
	Player black(board, "white", "e8");

	while (userInput != "checkMate")
	{
		
		if(userInput != "checkMate")
	}
}
